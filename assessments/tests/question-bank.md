# Примеры тестовых заданий

Файл содержит примеры заданий из исходных материалов и не является закрытым экзаменационным банком. Блок `DL-1.2` исключён как не относящийся к результатам дисциплины. Перед включением вопроса в фактический вариант преподаватель проверяет его соответствие модулю и отсутствие неоднозначных ответов.

## Исключённый исходный банк 1

Первый исходный банк не включён в применяемый набор: его нумерация и разметка множественных ответов не позволяют надёжно отделить ключи без предметной ручной проверки.

## Исходный банк 2

Использованный промпт:
В приложенном файле приведена структура дисциплины с указанием блоков дисциплины и содержание блоков для каждого блока приведены компетенции и индикаторы, которые соответствуют блоку. Для каждого указанного в таблице индикатора составь по 10 закрытых тестовых вопросов. Закрытый вопрос должен иметь 4 или более ответов, один или несколько ответов должны быть правильными. Выдели правильные ответы жирным шрифтом. Содержание вопросов должно соответствовать по смыслу названию блока, компетенции, индикатору и умениям из последнего столбца. Уровень вопросов должен быть продвинутым.

Модуль 1: Введение в компьютерное зрение и основы глубокого обучения
Индикатор: DL-3.1. Применяет (проводя выбор и эксперименты) известные алгоритмы и библиотеки компьютерного зрения, предобученные глубокие нейросетевые модели для прикладных задач анализа изображений и видеопотока, при необходимости дообучая и валидируя на собственных наборах данных (Уровень: Базовый)
Вопрос 1:
При настройке пайплайна аугментации данных для обучения модели детекции объектов на ограниченном датасете (менее 1000 изображений) с использованием библиотеки Albumentations, какая из следующих стратегий является наиболее эффективной для повышения обобщающей способности модели без значительного увеличения времени обучения?
A) Применение только геометрических преобразований (поворот на 90°, зеркальное отражение) для сохранения формы объектов.
B) Использование цветовых преобразований (изменение яркости, контраста, добавление шума) в сочетании с геометрическими, имитируя реальные условия съемки.
C) Применение сложных преобразований, таких как CutOut или GridMask, для имитации окклюзий.
D) Использование только преобразований, которые не изменяют разметку (bounding boxes), чтобы избежать ошибок аннотации.

Вопрос 2:
При использовании предобученной модели ResNet50 из библиотеки torchvision для классификации изображений в новом домене (например, медицинские снимки), что из перечисленного является критически важным для успешного применения Transfer Learning с сохранением высокого качества?
A) Обучение всей модели с нуля на новом датасете, игнорируя предобученные веса.
B) Замена последнего полносвязного слоя на новый, соответствующий числу классов, и дообучение только этого слоя с высокой скоростью обучения.
C) Замена последнего полносвязного слоя и дообучение всей модели с низкой скоростью обучения, используя предобученные веса в качестве инициализации.
D) Удаление всех слоев классификации и добавление новых случайно инициализированных слоев, замораживание остальной части сети.

Вопрос 3:
При разработке системы распознавания номерных знаков на видеопотоке с камер наблюдения, какая комбинация инструментов и подходов обеспечит наиболее эффективное решение с точки зрения скорости обработки и точности?
A) Использование OpenCV для извлечения кадров и классического метода OCR (Tesseract) для распознавания каждого кадра без предварительной детекции.
B) Применение YOLOv8 из библиотеки Ultralytics для детекции автомобилей и их номеров, и последующее использование CNN для классификации символов на каждом обнаруженном номере.
C) Использование предобученной модели из TensorFlow Hub для классификации всего изображения без выделения номеров.
D) Применение каскадного классификатора Хаара для детекции номеров и использование SVM для распознавания символов.

Вопрос 4:
Вы запускаете инференс предобученной модели на GPU. Обнаружено, что время обработки одного изображения составляет 50 мс. Для повышения пропускной способности до 1000 изображений в секунду, какое из следующих действий будет наиболее эффективным?
A) Уменьшение размера входного изображения до минимально допустимого (например, 224x224) для всех задач.
B) Переход на CPU, так как он лучше справляется с последовательными задачами.
C) Использование пакетной обработки (batch inference) с размером батча, оптимальным для вашей GPU, и оптимизация модели с помощью ONNX Runtime или TensorRT.
D) Увеличение числа потоков (threads) в Python с помощью модуля threading.

Вопрос 5:
При сохранении и загрузке моделей в PyTorch, какой метод гарантирует максимальную воспроизводимость результатов и гибкость для дальнейшего дообучения?
A) torch.save(model, 'model.pth') — сохранение всей модели целиком.
B) torch.save(model.state_dict(), 'model_weights.pth') — сохранение только словаря весов.
C) torch.jit.script(model) — сохранение модели в формате TorchScript.
D) torch.onnx.export(model, dummy_input, "model.onnx") — экспорт в формат ONNX.

Вопрос 6:
В контексте детекции объектов с использованием YOLO, какой параметр напрямую контролирует минимальный порог уверенности, необходимый для того, чтобы обнаружение было включено в итоговый результат, и наиболее сильно влияет на компромисс между полнотой и точностью?
A) iou_thres (Intersection over Union threshold) — порог для подавления не-максимумов (NMS).
B) conf_thres (Confidence threshold) — порог объектной уверенности.
C) agnostic_nms — флаг для класса-агностик NMS.
D) max_det — максимальное количество обнаружений на изображение.

Вопрос 7:
Для решения задачи сегментации дорожной разметки на видеопотоке в реальном времени, какую архитектуру предобученной модели наиболее целесообразно использовать и дообучить, учитывая требования к высокой частоте кадров?
A) Mask R-CNN с бэкбоном ResNet101.
B) DeepLabV3 с бэкбоном ResNet50.
C) U-Net с легковесным энкодером, например, MobileNetV3.
D) YOLOv8-seg.

Вопрос 8:
Вы хотите дообучить предобученную модель Vision Transformer (ViT) на небольшом наборе данных (500 изображений) для классификации редких видов птиц. Какая стратегия дообучения с наименьшей вероятностью приведет к катастрофическому забыванию (catastrophic forgetting) и переобучению?
A) Дообучение всей модели с высокой скоростью обучения (lr=1e-3) без регуляризации.
B) Дообучение только последнего классификационного слоя (head) с низкой скоростью обучения (lr=1e-4), заморозив все блоки трансформера.
C) Использование Parameter-Efficient Fine-Tuning (PEFT), например, LoRA (Low-Rank Adaptation), с низким рангом (rank=4) и обучение только добавленных параметров.
D) Увеличение размера изображения до максимума, чтобы модель могла извлечь больше признаков.

Вопрос 9:
При обработке видеопотока, извлекая кадры с помощью OpenCV, вы заметили, что обработка каждого кадра по отдельности приводит к пропуску кадров (fps ниже реального). Какая техника позволит наиболее эффективно обрабатывать видео с сохранением временных зависимостей?
A) Использование многопоточности для параллельной загрузки и обработки кадров с очередью.
B) Пропуск каждого второго кадра для снижения нагрузки.
C) Использование алгоритмов трекинга, чтобы не обрабатывать каждый кадр детектором, а обновлять позиции объектов.
D) Использование 3D CNN, которые обрабатывают сразу группу кадров.

Вопрос 10:
Вы сравниваете производительность двух моделей детекции объектов: YOLOv8n (nano) и YOLOv8x (extra-large) на одном и том же датасете. Модель YOLOv8n показывает mAP 0.45 и время инференса 10 мс, а YOLOv8x — mAP 0.52 и время 50 мс. Для системы, работающей на Edge-устройстве с ограниченными вычислительными ресурсами и требованием к частоте кадров 30 FPS, какую модель следует выбрать?
A) YOLOv8x, так как она точнее, а разницу в 40 мс можно компенсировать использованием более мощного GPU.
B) YOLOv8n, так как она обеспечивает достаточную скорость (100 FPS) и приемлемую точность для большинства Edge-приложений.
C) Провести более глубокий анализ и рассмотреть возможность использования модели YOLOv8s (small) как компромиссный вариант, после ее дообучения.
D) Выбрать YOLOv8n и использовать методы сжатия, такие как прунинг, для дополнительного ускорения.

Индикатор: PL-1.1. Разрабатывает и отлаживает прикладные решения разной сложности и для разного круга конечных пользователей с использованием языка программирования Python, тестирует, испытывает и оценивает качество таких решений (Уровень: Базовый)
Вопрос 1:
При разработке скрипта для автоматической обработки большого количества CSV-файлов (более 1 ГБ) в Python, какой подход к чтению и обработке данных минимизирует использование оперативной памяти?
A) Использование pandas.read_csv() для загрузки всего файла в DataFrame.
B) Использование стандартного open() и чтение файла построчно с помощью цикла.
C) Использование библиотеки csv с построчным чтением через csv.reader.
D) Использование библиотеки pandas с параметром chunksize.

Вопрос 2:
Вы разрабатываете веб-сервис на базе FastAPI для подачи предсказаний ML-модели. Какой метод наиболее эффективен для обработки одновременно поступающих запросов и обеспечения масштабируемости?
A) Использование requests для синхронной отправки запросов.
B) Использование асинхронных функций (async def) с await для операций ввода-вывода.
C) Запуск нескольких экземпляров приложения без оркестрации.
D) Использование многопоточности для каждой операции ввода-вывода.

Вопрос 3:
При работе с JSON-файлами большого размера (сотни МБ), какой из методов предпочтительнее для парсинга, чтобы избежать переполнения памяти (MemoryError)?
A) json.load() для загрузки всего файла в объект Python.
B) json.loads() для чтения строки целиком.
C) Использование ijson для потокового парсинга.
D) Преобразование JSON в CSV и использование pandas.

Вопрос 4:
При использовании библиотеки requests для отправки большого количества HTTP-запросов к API, какая техника значительно улучшит производительность и сократит общее время выполнения?
A) Использование сессии (requests.Session) для повторного использования TCP-соединения.
B) Отправка запросов последовательно в цикле.
C) Использование библиотеки requests в синхронном режиме.
D) Установка таймаута (timeout) для каждого запроса.

Вопрос 5:
Какой механизм в Python наиболее подходит для разделения длительных вычислений на несколько параллельно выполняющихся процессов, чтобы обойти ограничения GIL (Global Interpreter Lock)?
A) Модуль threading.
B) Модуль asyncio.
C) Модуль multiprocessing.
D) Модуль concurrent.futures с ThreadPoolExecutor.

Вопрос 6:
Вы написали скрипт для сбора данных из нескольких источников, но он часто падает из-за сетевых ошибок. Какой паттерн обработки ошибок обеспечит наибольшую устойчивость и завершит выполнение скрипта?
A) Использование try...except для каждого запроса и вывод ошибки в консоль, продолжение работы.
B) Использование цикла с повторными попытками (retry) и экспоненциальной задержкой (exponential backoff).
C) Игнорирование ошибок и переход к следующему источнику.
D) Остановка выполнения скрипта при первой же ошибке.

Вопрос 7:
Для логирования в серверном приложении на Python, которое должно работать 24/7, какой подход к логированию наиболее правильный для мониторинга и отладки в продакшене?
A) Использование print() для вывода сообщений в консоль.
B) Использование встроенного модуля logging с настройкой форматтера, уровней логирования и ротации файлов.
C) Сохранение всех логов в один файл без ротации.
D) Логирование только ошибок, игнорируя информационные сообщения.

Вопрос 8:
В контексте тестирования качества кода, что означает утверждение assert в Python?
A) Ключевое слово для объявления переменной.
B) Инструмент для отладки, который проверяет условие и вызывает AssertionError, если оно ложно.
C) Способ обработки исключений в блоке try...except.
D) Инструмент для профилирования производительности.

Вопрос 9:
Вы разрабатываете библиотеку для работы с изображениями, которую будут использовать другие разработчики. Какой метод лучше всего подходит для документирования ваших функций и классов?
A) Добавление комментариев внутри кода.
B) Использование докстрингов (docstrings) и генерация документации с помощью Sphinx.
C) Создание отдельного файла README.md.
D) Написание подробного блога о библиотеке.

Вопрос 10:
При создании CLI-приложения для обработки данных, какая библиотека обеспечит наиболее удобный и стандартизированный способ парсинга аргументов командной строки?
A) sys.argv.
B) argparse.
C) click.
D) optparse.

Индикатор: PL-1.2. Осуществляет выбор инструментов разработки на Python, приемлемых для создания прикладной системы обработки научных данных, машинного обучения и визуализации с заданными требованиями (Уровень: Базовый)
Вопрос 1:
Для выполнения сложных матричных операций с использованием GPU в задаче глубокого обучения, какую библиотеку следует предпочесть?
A) NumPy.
B) SciPy.
C) PyTorch или TensorFlow.
D) Pandas.

Вопрос 2:
Для создания интерактивных и масштабируемых визуализаций с большим количеством точек данных (например, 10 миллионов), какую библиотеку наиболее целесообразно использовать?
A) Matplotlib.
B) Seaborn.
C) Plotly.
D) Bokeh или Datashader.

Вопрос 3:
В задаче оптимизации гиперпараметров ML-модели, какую библиотеку следует использовать для проведения эффективного поиска по пространству параметров с использованием байесовской оптимизации?
A) Scikit-learn.
B) Optuna.
C) NumPy.
D) Pandas.

Вопрос 4:
Для работы с разреженными матрицами (sparse matrices) в задачах обработки текстов, какая библиотека предоставляет наиболее оптимизированные структуры данных?
A) NumPy.
B) SciPy (модуль scipy.sparse).
C) PyTorch.
D) Dask.

Вопрос 5:
При необходимости обработки датасета, который не помещается в оперативную память (более 100 ГБ), какой стек инструментов наиболее подходит для его обработки на одном сервере?
A) Pandas с параметром chunksize.
B) Dask DataFrame.
C) PySpark на кластере.
D) Использование joblib для параллельной обработки.

Вопрос 6:
Для визуализации статистических данных и построения сложных статистических графиков (например, violin plot, joint plot) с высокой эстетикой, какая библиотека является предпочтительным выбором?
A) Matplotlib.
B) Seaborn.
C) Plotly Express.
D) ggplot (plotnine).

Вопрос 7:
При разработке прототипа модели машинного обучения на небольших данных, где ключевым требованием является быстрота разработки и наличие большого количества реализованных алгоритмов, какой инструмент следует использовать в первую очередь?
A) TensorFlow.
B) PyTorch.
C) Scikit-learn.
D) XGBoost.

Вопрос 8:
Для обработки данных с нечисловыми типами (категориальные признаки, тексты) и их преобразования в числовые векторы для подачи в ML-модель, какой модуль библиотеки Scikit-learn предназначен для этих целей?
A) sklearn.preprocessing.
B) sklearn.feature_extraction.
C) sklearn.decomposition.
D) sklearn.pipeline.

Вопрос 9:
При необходимости быстрого прототипирования и визуализации архитектуры нейронной сети, какой фреймворк предоставляет высокоуровневый Keras API?
A) PyTorch.
B) TensorFlow (с модулем tf.keras).
C) JAX.
D) MXNet.

Вопрос 10:
В задаче классификации текстов, где требуется извлечь из документа N наиболее важных слов (токенов) для построения TF-IDF матрицы, какой класс библиотеки Scikit-learn следует использовать?
A) CountVectorizer.
B) TfidfTransformer.
C) TfidfVectorizer.
D) HashingVectorizer.

Модуль 2: Глубокое обучение в компьютерном зрении: методологии обучения и метрики
Индикатор: DL-3.1. Применяет (проводя выбор и эксперименты) известные алгоритмы и библиотеки компьютерного зрения, предобученные глубокие нейросетевые модели для прикладных задач анализа изображений и видеопотока, при необходимости дообучая и валидируя на собственных наборах данных (Уровень: Средний)
Вопрос 1:
Вы дообучаете предобученную модель ResNet50 на датасете из 5000 медицинских снимков для классификации трех типов рака. Вы заметили, что модель хорошо работает на обучающей выборке, но показывает низкую точность на валидационной. Какая стратегия наиболее эффективна для решения этой проблемы?
A) Увеличить количество эпох обучения.
B) Применить сложную аугментацию данных (например, ElasticTransform, GridDistortion) для увеличения разнообразия тренировочных данных.
C) Использовать модель большего размера, например, ResNet152.
D) Уменьшить размер батча (batch size).

Вопрос 2:
При проведении экспериментов с различными архитектурами для задачи детекции объектов, вы сравниваете YOLOv8 и Faster R-CNN. В чем заключается основное преимущество YOLOv8 перед Faster R-CNN в контексте времени инференса?
A) YOLOv8 использует более точный метод Region Proposal Network (RPN).
B) YOLOv8 выполняет детекцию в один проход (one-stage), в то время как Faster R-CNN требует двух этапов.
C) YOLOv8 использует более глубокий бэкбон, чем Faster R-CNN.
D) YOLOv8 не использует Non-Maximum Suppression (NMS).

Вопрос 3:
Для создания сквозного пайплайна обработки видеопотока в реальном времени, какая архитектура наиболее подходит для одновременного решения задач детекции, трекинга и классификации действий (action recognition)?
A) Комбинация YOLO для детекции, DeepSORT для трекинга и отдельной 3D CNN для классификации действий на основе треков.
B) Использование модели, объединяющей Vision Transformer (ViT) с рекуррентными слоями (LSTM) для обработки последовательности кадров.
C) Кадровая обработка с использованием ResNet без учета временных связей.
D) Использование YOLO для детекции и SORT для трекинга.

Вопрос 4:
Вы решили использовать библиотеку Albumentations для аугментации данных. Какая комбинация трансформаций наиболее релевантна для модели, которая должна быть устойчива к изменению освещения и небольшим поворотам объектов на изображении?
A) HorizontalFlip, VerticalFlip, Rotate (90, 180, 270).
B) RandomBrightnessContrast, HueSaturationValue, ShiftScaleRotate.
C) GaussNoise, MotionBlur, OpticalDistortion.
D) Cutout, CoarseDropout, GridMask.

Вопрос 5:
Вы дообучаете предобученную модель на новом датасете, который значительно отличается от ImageNet (например, аэрофотоснимки). Какая стратегия оптимизации гиперпараметров будет наиболее разумной?
A) Использовать ту же скорость обучения, что и при обучении на ImageNet.
B) Использовать значительно более высокую скорость обучения для быстрой адаптации.
C) Использовать значительно более низкую скорость обучения (lr=1e-5) и возможно, разморозить слои бэкбона постепенно (progressive unfreezing).
D) Использовать оптимизатор Adam с настройками по умолчанию.

Вопрос 6:
Какая из перечисленных техник позволяет обрабатывать видео как последовательность, используя 3D свертки, и какой основной недостаток у этого подхода?
A) 3D CNN; недостаток - высокая вычислительная сложность.
B) ConvLSTM; недостаток - трудность обучения долгосрочным зависимостям.
C) 3D CNN; недостаток - невозможность использовать предобученные веса.
D) Смешанная модель (CNN + RNN); недостаток - сложность реализации.

Вопрос 7:
При сравнении производительности различных предобученных моделей для задачи классификации, вы решили использовать метрику Accuracy. В каком случае Accuracy может быть вводящей в заблуждение метрикой?
A) Когда количество классов большое.
B) Когда датасет сбалансирован.
C) Когда датасет несбалансирован (например, один класс составляет 95% данных).
D) Когда модель быстро обучается.

Вопрос 8:
Вы работаете с задачей видеодетекции и хотите улучшить качество обнаружения мелких объектов. Какая стратегия наиболее эффективна в этом случае?
A) Увеличение разрешения входных кадров (например, с 640x640 до 1280x1280).
B) Использование модели с большим количеством параметров.
C) Применение техники, такой как Feature Pyramid Network (FPN) или BiFPN, для объединения признаков с разных уровней.
D) Увеличение размера батча.

Вопрос 9:
В процессе дообучения модели на новом датасете, вы решили использовать стратегию fine-tuning, разморозив все слои. Какое влияние это окажет на скорость обучения и риск переобучения по сравнению с feature extraction?
A) Скорость обучения будет выше, риск переобучения ниже.
B) Скорость обучения будет ниже, риск переобучения выше.
C) Скорость обучения будет ниже, риск переобучения ниже.
D) Скорость обучения будет выше, риск переобучения выше.

Вопрос 10:
При использовании библиотеки PyTorch, вы хотите сохранить лучшую модель в процессе обучения на основе валидационной метрики. Какой подход обеспечит это?
A) Сохранение модели только после завершения всех эпох.
B) Использование ModelCheckpoint callback (в PyTorch Lightning) или ручное сохранение при улучшении валидационной метрики.
C) Сохранение модели каждые N эпох.
D) Сохранение модели с наименьшим значением функции потерь на обучающей выборке.

Модуль 3 и 4: Применение СНС для задач классификации, сегментации и детекции; Применение архитектур для обработки видео
Индикатор: DL-3.1. Применяет (проводя выбор и эксперименты) известные алгоритмы и библиотеки компьютерного зрения, предобученные глубокие нейросетевые модели для прикладных задач анализа изображений и видеопотока, при необходимости дообучая и валидируя на собственных наборах данных (Уровень: Продвинутый)
Вопрос 1:
При разработке комплексного решения для многокамерной видеоаналитики с высокими требованиями к производительности, какой подход к распределению вычислительной нагрузки между CPU и GPU является наиболее эффективным?
A) Выполнение всех операций (декодирование, предобработка, инференс) последовательно на GPU.
B) Выполнение декодирования и предобработки на CPU с использованием многопоточности, а инференса — на GPU с использованием пакетной обработки, организованной через очередь.
C) Использование только CPU для минимизации задержек при передаче данных.
D) Выполнение инференса на нескольких GPU с использованием DataParallel, а предобработки — на CPU.

Вопрос 2:
Вы дообучаете модель YOLOv8 на кастомном датасете для детекции дефектов на производственной линии. Качество модели на валидации (mAP) достигло плато, но вы уверены, что можно улучшить результат. Какая из следующих стратегий наиболее вероятно приведет к улучшению, если учесть, что датасет содержит около 2000 аннотированных изображений?
A) Использование AutoML для поиска оптимальных гиперпараметров обучения.
B) Использование методов активного обучения (Active Learning) для выбора наиболее информативных примеров для доразметки.
C) Увеличение размера модели (переход с YOLOv8m на YOLOv8l) без изменения других параметров.
D) Применение техники тестового увеличения времени (Test Time Augmentation, TTA) на этапе валидации и инференса.

Вопрос 3:
При интеграции модели детекции объектов в высоконагруженный веб-сервис, какая архитектура обеспечит наилучшую масштабируемость и устойчивость к пиковым нагрузкам?
A) Развертывание модели как однопоточного приложения на мощном сервере.
B) Развертывание модели в Docker-контейнере и использование оркестратора (например, Kubernetes) для горизонтального масштабирования.
C) Использование бессерверной архитектуры (Serverless, AWS Lambda) для обработки каждого запроса.
D) Интеграция модели в базу данных с использованием хранимых процедур.

Вопрос 4:
Для задачи анализа поведения человека на видео (activity recognition), какую архитектуру следует выбрать, чтобы эффективно учитывать как пространственные, так и временные зависимости, и при этом обеспечить возможность использования предобученных моделей для извлечения пространственных признаков?
A) 3D CNN, обученная с нуля на больших видео-датасетах.
B) Двухпоточная сеть (Two-Stream Network): один поток для RGB-кадров (предобученный CNN), другой для оптического потока.
C) ConvLSTM, где энкодер — предобученный CNN, а LSTM — для обработки временной последовательности.
D) Vision Transformer (ViT) с дополнительным позиционным кодированием, обученный на кадрах.

Вопрос 5:
Вы разрабатываете систему для отслеживания объектов в реальном времени с камеры на дроне. Какой алгоритм трекинга лучше всего подходит, если у вас нет детектора, работающего в реальном времени, и вы хотите минимизировать количество пропущенных кадров?
A) SORT (Simple Online and Realtime Tracking), основанный на детекциях.
B) DeepSORT, использующий внешний Re-ID модуль.
C) Kernelized Correlation Filter (KCF) или Median Flow, как метод однокадрового трекинга (single object tracker).
D) Tracktor, который не использует отдельный детектор.

Вопрос 6:
Вы проводите A/B-тестирование двух моделей детекции объектов в продуктивной среде. Модель A показывает более высокую mAP, но медленнее (50 мс против 20 мс). Модель B быстрее, но имеет больше ложных срабатываний (false positives). Какой подход позволит принять взвешенное решение о выборе модели для продакшена?
A) Всегда выбирать модель с более высокой mAP, так как точность важнее скорости.
B) Всегда выбирать модель с меньшим временем инференса, так как пользователи не любят ждать.
C) Рассчитать комплексную метрику эффективности, учитывающую бизнес-показатели, например, стоимость ложного срабатывания и пропуска цели, и на основе этого выбрать модель.
D) Провести опрос пользователей о том, что они предпочитают.

Вопрос 7:
В задаче построения сквозного пайплайна обработки видео, какая библиотека обеспечивает наибольшую гибкость для создания сложных потоков обработки данных с возможностью использования GPU-ускорения (CUDA)?
A) OpenCV с Python привязкой.
B) PyTorch с модулем torchvision.
C) NVIDIA DALI (Data Loading Library).
D) FFmpeg.

Вопрос 8:
При дообучении модели YOLOv8 на собственном датасете, вы замечаете, что модель хорошо детектирует крупные объекты, но почти не видит мелкие. Какая из следующих модификаций архитектуры или процесса обучения наиболее эффективно решит эту проблему?
A) Увеличение размера входного изображения при обучении.
B) Использование модели с более широким бэкбоном, например, YOLOv8x.
C) Изменение функции потерь, чтобы увеличить вес ошибок для мелких объектов (например, увеличение веса для bounding boxes маленького размера).
D) Использование более строгого порога IoU для Non-Maximum Suppression (NMS).

Вопрос 9:
Ваша модель детекции объектов должна работать на встраиваемой системе (Raspberry Pi). Какие методы оптимизации вы примените в первую очередь для достижения приемлемой частоты кадров (FPS)?
A) Квантование весов модели до INT8.
B) Использование модели с меньшим количеством параметров (например, YOLOv8n вместо YOLOv8m).
C) Прунинг (pruning) модели для удаления малозначимых нейронов.
D) Использование TensorFlow Lite (TFLite) или ONNX Runtime.

Вопрос 10:
Вы проектируете архитектуру системы ИИ для анализа видеопотоков с камер в городе (умный город). Задача: детекция ДТП, распознавание номеров, подсчет транспорта. Какой подход к организации вычислительной инфраструктуры наиболее эффективен с точки зрения баланса стоимости и производительности?
A) Централизованная обработка всех каналов на мощном сервере с несколькими GPU.
B) Использование Edge-устройств для предварительной обработки и детекции на каждой камере, а на центральный сервер отправлять только метаданные (координаты, номера) для более сложного анализа.
C) Хранение всех видеозаписей в облаке и их обработка по требованию.
D) Использование только CPU для обработки, чтобы избежать затрат на GPU.

## Исходный банк 3

**Нормативная привязка:** модуль 4, `DL-2.1`, средний уровень.

Тестовые задания для автоматической проверки теории
Тема: Диффузионные модели в компьютерном зрении
Количество вопросов: 10
Формат: Один или несколько правильных ответов (ключ к ответам приведен в конце).

1. Какой базовый математический аппарат лежит в основе процессов прямой и обратной диффузии в модели DDPM?
A) Скрытые марковские модели (HMM)
B) Дискретные марковские цепи
C) Стохастические дифференциальные уравнения Леви
D) Генетические алгоритмы отбора
2. Что является целью обучения нейросети U-Net в классической архитектуре DDPM?
A) Прямое предсказание чистого исходного изображения  на каждом шаге
B) Классификация стиля входного изображения
C) Предсказание случайного гауссова шума, добавленного к изображению на текущем шаге 
D) Вычисление матрицы взаимного внимания (Cross-Attention) между соседними пикселями
3. В модели Stable Diffusion (Latent Diffusion) процессы зашумления и денойзинга перенесены в латентное пространство. Какой блок архитектуры отвечает за переход из пространства пикселей в латентное пространство и обратно?
A) CLIP Text Encoder
B) Variational Autoencoder (VAE)
C) ViT (Vision Transformer)
D) Система замороженных сверток ResNet-50
4. Какое утверждение относительно прямого процесса диффузии (Forward Process) является верным?
A) Каждый шаг требует обязательного запуска нейросети U-Net
B) Изображение на любом шаге  можно вычислить аналитически напрямую из , зная расписание дисперсии 
C) Процесс переводит случайный гауссов шум в структурированное изображение
D) Дисперсия добавляемого шума уменьшается до нуля при приближении к финальному шагу 
5. Параметр Guidance Scale (Classifier-Free Guidance, CFG) регулирует степень соответствия генерации текстовому промпту. Что происходит при установке слишком высоких значений CFG (например, CFG = 30.0)?
A) Изображение становится полностью случайным белым шумом
B) Модель начинает полностью игнорировать текстовый промпт
C) Картинка приобретает неестественную контрастность, артефакты перенасыщения (over-saturation) и палитры
D) Время генерации изображения увеличивается линейно в 30 раз
6. Студент увеличил параметр шагов денойзинга (Steps) в Euler Sampler с 20 до 500. Как это повлияет на процесс генерации Stable Diffusion v1.5?
A) Качество изображения гарантированно вырастет пропорционально росту шагов
B) Время генерации существенно увеличится, но качество изображения может выйти на плато или ухудшиться из-за особенностей сходимости сэмплера
C) Модель автоматически переключится в режим Inpainting
D) Вызов функции упадет с ошибкой нехватки видеопамяти (Out of Memory)
7. Какой механизм внутри U-Net диффузионной модели отвечает за интеграцию текстового описания (промпта) в генерируемые латентные признаки?
A) Self-Attention (Самопредставление)
B) Cross-Attention (Перекрестное внимание)
C) Поканальная конкатенация тензоров (Concat)
D) Элементное умножение (Hadamard product)
8. Для чего используется технология ControlNet в диффузионных моделях?
A) Для ускорения генерации за счет квантования весов U-Net в Int8
B) Для автоматического подбора негативных промптов
C) Для добавления жестких пространственных или геометрических условий (маски, контуры, позы) в процесс генерации
D) Для сжатия пикселей в латентный код вместо использования VAE
9. При выполнении задачи Inpainting параметр Strength (сила изменения) регулирует степень зашумления исходного изображения под маской. Каков будет результат работы пайплайна, если установить Strength = 0.05?
A) Область под маской изменится кардинально и полностью обновится согласно новому промпту
B) Модель проигнорирует маску и перегенерирует все изображение целиком
C) Область под маской практически не изменится, сохранив исходные пиксели с минимальными изменениями
D) Скрипт выдаст ошибку деления на ноль
10. Какую роль играет Negative Prompt (негативный промпт) при генерации изображений?
A) Он удаляет указанные объекты из латентного пространства, сдвигая вектор генерации в противоположную сторону от эмбеддингов нежелательных слов
B) Он уменьшает яркость и контрастность финального изображения
C) Он используется для обучения дискриминатора в архитектуре диффузии
D) Он блокирует генерацию NSFW-контента на уровне видеокарты
